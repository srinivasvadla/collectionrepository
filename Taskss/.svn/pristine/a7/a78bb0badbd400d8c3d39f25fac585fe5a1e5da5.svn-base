package com.srinu.classes;

// creating the class and objects
// we cannot start  the class name with uppercase and numbers 
public class FirstClass_05_2018 {
	public static void main(String[] args) 
	{

		// create the object
		FirstClass_05_2018 f = new FirstClass_05_2018();
		f.local_variables();

		// instance variables
		int local = 10;
		String name = "srinivas";
		long l = 1234522;
		float m = 1233f;
		double n = 342.5;

		System.out.println("instance variablesss int value is, " + local + " name is ,  " + name + " long value is,   " + l + "float values is,   " + m + "double is , " + n);
		
		// we cannot acess the local variables outside the method : hence  not possible 
		//System.out.println(i);
	}

	// local variables
		public void local_variables()
		{
		int i = 10;
		String name = "srinivas";
		long l = 1234522;
		float f = 1233f;
		double d = 342.5;

		System.out.println("local variablesss  " + i + "  " + name + " " + l + " " + f + " " + d);
		
		// we cannot acess the instance variables  inside the method
		//System.out.println(local);
		
	}

}
